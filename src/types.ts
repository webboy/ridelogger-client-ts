export interface Country {
    id: number;
    name: string;
    code: string;
}

export interface Vehicle {
    id: number;
    make: string;
    model: string;
    year: number;
}

// ... other shared interfaces